// Warmth variant module — Frontend Eng (Warmth) owns this.
// Exports { mount, unmount } per architecture-spec §1 lifecycle contract.
// Gallery = slider with rope handle + first-person captions (meeting-minutes §C2).

export function mount(/* root */) {
  // TODO: slider-with-rope-handle init, scroll-triggered reveals, etc.
}

export function unmount(/* root */) {
  // TODO: detach listeners.
}
